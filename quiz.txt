实验1-1：计算多项式：f(x)=ax^3+bx^2+cx+d的值，其中x，a，b，c，d值由键盘输入，需有必要文字提示，输出的f(x)值保留小数点后两位。
实验1-2：已知三角形的三边长分别为a,b,c，求其面积s。其中a,b,c值由键盘输入，需有必要文字提示，如果输入的三条边长不符合构成三角形的条件，则输出“三边边长不符合三角形边长关系”，否则输出的面积s值（s值保留小数点后一位）。
实验2-1：有如下分段函数：y = 2x (x<=10); y = 2x+1 (10<x<50); y = x/2 + 0.1 (x>=50). 其中x值由键盘输入，求y值并输出（保留小数点后1位）。
实验2-2：字符型字符串密码转换：密码规律:如果原字母是大写字母，则先将其变成小写字母，再用该小写字母后面第4个字母代替原来的字母；如果原字母是小写字母，则先将其变成大写字母，再用该大写字母后面第4个字母代替原来的字母。若某字母后面剩余字母个数不足4个，则衔接至a或者A，例如x后面第4个字母为b（x后面的4个字母依次为y,z,a,b）。例如，原字母是“A”，则先将其变成“a”。“a”后面第4个字母是“e”，用“e”代替“A”。因此，“China”应译为“gLMRE”。
实验3-1：输入两个正整数，求两个数之间（含两个数）所有奇数之和、偶数之和。例如，两个正整数为3、7；则奇数之和为8（3+5），偶数之和为10（4+6）。
实验3-2：有一个分数序列:2/1,  3/2,  5/3,  8/5……求出这个数列的前20项之和。
实验4-1：从键盘输入10个整数，并求出这10个数的最大值及出现次数。
实验4-2：利用二维数组实现矩阵转置，键盘输入矩阵行列和元素，显示初始矩阵，将矩阵转置，显示转置后的矩阵。
//自实验5起，第2题均为选做
实验5-1：编写一个名为day_of_year(int year,int month,int day)的函数，参数分别是年，月，日，计算并返回这一天是该年的第几天（注意：程序应该考虑闰年情况）。
实验5-2：编写函数分别实现两个整数的加、减、乘、除四则运算，通过键盘输入两个整数运算的完整表达式，根据表达式运算符通过调用相应函数得出结果并显示。例如：通过键盘输入12+34，主程序将调用sum（int x，int y），屏幕输出46。
实验6-1：输入一条英文字符串，先通过程序去除字符串中重复字母并按字典序排列，然后通过递归程序调用形式实现这些字母的所有排列顺序显示。
实验6-2：用弦截法求解方程下列方程的根：要求编写三个函数实现各部分的功能: ① 函数f(x)：求x的函数:x3 - 5x2 +16x-80； ② 函数xpoint(x1,x2)：求弦与x轴交点X的x坐标 ；③ 函数root(x1, x2) ：求(x1, x2)区间的实根。
实验7-1：编写程序实现：输入字符串及准备删除的字符位置和个数，使用自编函数char*str_delete(char *s,int v,int w)从字符串s的第v个字符开始删除w个字符，将处理后的字符串首地址以函数返回值返回并输出显示处理后的字符串。
实验7-2：已知数组num[10]={9, 7, 6, 12, 44, 53, 32, 11, 29，X}，其中X为本人学号后两位。要求用指针的方法从10个数中找到最大值和最小值。要求编写函数：find_max(int *p, int n)  //n表示数组的大小；find_min(int *p, int n)  //n表示数组的大小
实验8-1：共有三名学生，每名学生有三门课成绩需要统计，要求采用结构体编程，依次输入三位学生的学号，姓名和三门课程的成绩，分别用函数计算出每个学生平均分、每门课的平均分、9个分数中最高的分数对应的学生和课程，将计算结果打印在屏幕上的同时写入文本文件进行存储。
实验8-2：某个寝室选举寝室长，有3位候选人，有6名同学参与投票，而且只能投一个人，要求采用结构体编写统计选票的程序。先输入候选人名字，再输入同学所投的候选人名字，最后根据每位候选人的得票数排序输出显示。
